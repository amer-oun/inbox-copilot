node_modules/
dist/
# The Prisma client, generated to an explicit path by `prisma generate`
# (see packages/db/prisma/schema.prisma). A build artefact, engines and all.
packages/db/generated/
# The engine + schema copied into the web app so Prisma finds them at runtime on Vercel
# (scripts/copy-prisma-engine.mjs, run by apps/web's build).
apps/web/generated/
.next/
.turbo/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local

.DS_Store
*.log

# Local AI assistant configuration. Kept on disk, never committed — these instruct a
# coding assistant, they are not project documentation. ARCHITECTURE.md, README.md and
# docs/ are the tracked docs; nothing here is referenced by them.
CLAUDE.md
CLAUDE.local.md
AGENTS.md
GEMINI.md
.claude/
.cursor/
.cursorrules
.continue/
.aider*
.windsurfrules
.clinerules
.github/copilot-instructions.md
.mcp.json
