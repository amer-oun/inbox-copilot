node_modules
dist
.next
.turbo
coverage
pnpm-lock.yaml
*.tsbuildinfo

# Generated by Prisma from schema.prisma, and edited by nobody.
packages/db/prisma/migrations

# Prose, hand-wrapped, and read as documents rather than as source. Prettier's
# Markdown printer reflows lists and inserts blank lines after headings, which is a
# large diff that changes no meaning — so the docs keep the wrapping their authors chose.
*.md
